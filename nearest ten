int main() {
    int n;
    scanf("%d", &n);

    int remainder = n % 10;
    int nearest_multiple;

    if (remainder >= 5) {
        nearest_multiple = n + (10 - remainder);
    } else {
        nearest_multiple = n - remainder;
    }

    printf("%d\n", nearest_multiple);
    return 0;
}
